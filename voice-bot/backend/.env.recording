# Call Recording Settings - Add these to your .env file

# Enable provider-side recording (Twilio) for all calls
RECORD_CALLS=true

# Recording channels: 'dual' for speaker separation or 'mono' for single channel
RECORDING_CHANNELS=dual

# Webhook path where Twilio posts recording status
RECORDING_STATUS_PATH=api/recording-status
